#!/usr/bin/env bash
set -euo pipefail

if [[ $# -eq 0 ]]; then
	exec limactl shell --workdir /home/dev dev zsh -lic 'if command -v zellij >/dev/null 2>&1; then exec zellij attach -c dev; fi; exec zsh -l'
fi

exec limactl shell --workdir /home/dev dev "$@"
