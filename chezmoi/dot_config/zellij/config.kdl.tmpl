// ~/.config/zellij/config.kdl

{{- $needsTaskwarrior := false -}}
{{- if hasKey . "needs_taskwarrior" }}{{- $needsTaskwarrior = .needs_taskwarrior }}{{- end }}

theme "catppuccin-mocha"

simplified_ui true
pane_frames false
default_mode "locked"
copy_on_select true
// NB: intentionally no `copy_command`. Zellij treats copy_command as
// "I'll handle every copy myself" and stops passing OSC 52 sequences
// through from panes to the enclosing terminal. Without this line,
// zellij passes OSC 52 through untouched, so nvim yanks / any shell
// command piped into ~/.local/bin/osc52-copy lands in the macOS
// clipboard via WezTerm. Trade-off: zellij's own mouse-drag selection
// stays in its internal paste buffer (no macOS clipboard sync).
scrollback_editor "nvim"
mouse_mode true
show_startup_tips false
web_server false
web_sharing "off"

keybinds {
    unbind "Ctrl g"

    normal {
        bind "Alt l" { SwitchToMode "Locked"; }

        // Floating tools
        bind "Alt f" {
            Run "zsh" "-l" {
                floating true
                x "7%"
                y "7%"
                width "86%"
                height "86%"
                close_on_exit true
            }
        }
        bind "Alt g" {
            Run "lazygit" {
                floating true
                x "7%"
                y "7%"
                width "86%"
                height "86%"
                close_on_exit true
            }
        }
        bind "Alt e" {
            Run "zsh" "-lc" "exec yazi \"$PWD\"" {
                floating true
                x "7%"
                y "7%"
                width "86%"
                height "86%"
                close_on_exit true
            }
        }
        bind "Alt r" {
            Run "scooter" {
                floating true
                x "7%"
                y "7%"
                width "86%"
                height "86%"
                close_on_exit true
            }
        }
{{- if $needsTaskwarrior }}
        bind "Alt w" {
            Run {{ joinPath .chezmoi.homeDir ".local" "bin" ",task-popup" | quote }} {
                cwd "."
                floating true
                x "7%"
                y "7%"
                width "86%"
                height "86%"
                close_on_exit true
            }
        }
{{- end }}
        bind "Alt ?" {
            Run {{ joinPath .chezmoi.homeDir ".local" "bin" ",cheatsheet" | quote }} {
                cwd "."
                floating true
                x "7%"
                y "7%"
                width "86%"
                height "86%"
                close_on_exit true
            }
        }

        // Pane actions
        bind "Alt h" { MoveFocus "Left"; }
        bind "Alt j" { MoveFocus "Down"; }
        bind "Alt k" { MoveFocus "Up"; }
        // Alt l handled above (mode switch) — move-focus-right in locked only
        bind "Alt v" { NewPane "Right"; }
        bind "Alt o" { TogglePaneEmbedOrFloating; }
        bind "Alt z" { ToggleFocusFullscreen; }
        bind "Alt b" { SwitchToMode "RenamePane"; }

        // Tab actions
        bind "Alt t" { NewTab; }
        bind "Alt m" { SwitchToMode "RenameTab"; }
        bind "Alt n" { GoToNextTab; }
        bind "Alt p" { GoToPreviousTab; }
        bind "Alt q" { CloseTab; }
        bind "Alt d" { Detach; }
        bind "Alt 1" { GoToTab 1; }
        bind "Alt 2" { GoToTab 2; }
        bind "Alt 3" { GoToTab 3; }
        bind "Alt 4" { GoToTab 4; }
        bind "Alt 5" { GoToTab 5; }
        bind "Alt 6" { GoToTab 6; }
        bind "Alt 7" { GoToTab 7; }
        bind "Alt 8" { GoToTab 8; }
        bind "Alt 9" { GoToTab 9; }
    }

    locked {
        bind "Alt a" { SwitchToMode "Normal"; }

        // Floating tools
        bind "Alt f" {
            Run "zsh" "-l" {
                floating true
                x "7%"
                y "7%"
                width "86%"
                height "86%"
                close_on_exit true
            }
        }
        bind "Alt g" {
            Run "lazygit" {
                floating true
                x "7%"
                y "7%"
                width "86%"
                height "86%"
                close_on_exit true
            }
        }
        bind "Alt e" {
            Run "zsh" "-lc" "exec yazi \"$PWD\"" {
                floating true
                x "7%"
                y "7%"
                width "86%"
                height "86%"
                close_on_exit true
            }
        }
        bind "Alt r" {
            Run "scooter" {
                floating true
                x "7%"
                y "7%"
                width "86%"
                height "86%"
                close_on_exit true
            }
        }
{{- if $needsTaskwarrior }}
        bind "Alt w" {
            Run {{ joinPath .chezmoi.homeDir ".local" "bin" ",task-popup" | quote }} {
                cwd "."
                floating true
                x "7%"
                y "7%"
                width "86%"
                height "86%"
                close_on_exit true
            }
        }
{{- end }}
        bind "Alt ?" {
            Run {{ joinPath .chezmoi.homeDir ".local" "bin" ",cheatsheet" | quote }} {
                cwd "."
                floating true
                x "7%"
                y "7%"
                width "86%"
                height "86%"
                close_on_exit true
            }
        }

        // Pane actions
        bind "Alt Left"  { MoveFocus "Left"; }
        bind "Alt Down"  { MoveFocus "Down"; }
        bind "Alt Up"    { MoveFocus "Up"; }
        bind "Alt Right" { MoveFocus "Right"; }
        bind "Alt h" { NewPane "Down"; }
        bind "Alt v" { NewPane "Right"; }
        bind "Alt s" { NewPane "stacked"; }
        bind "Alt x" { CloseFocus; }
        bind "Alt o" { TogglePaneEmbedOrFloating; }
        bind "Alt z" { ToggleFocusFullscreen; }
        bind "Alt b" { SwitchToMode "RenamePane"; }

        // Tab actions
        bind "Alt t" { NewTab; }
        bind "Alt m" { SwitchToMode "RenameTab"; }
        bind "Alt n" { GoToNextTab; }
        bind "Alt p" { GoToPreviousTab; }
        bind "Alt q" { CloseTab; }

        // Session actions
        bind "Alt d" { Detach; }
        bind "Alt 1" { GoToTab 1; }
        bind "Alt 2" { GoToTab 2; }
        bind "Alt 3" { GoToTab 3; }
        bind "Alt 4" { GoToTab 4; }
        bind "Alt 5" { GoToTab 5; }
        bind "Alt 6" { GoToTab 6; }
        bind "Alt 7" { GoToTab 7; }
        bind "Alt 8" { GoToTab 8; }
        bind "Alt 9" { GoToTab 9; }
    }

    renametab {
        bind "Ctrl c" { SwitchToMode "Locked"; }
        bind "Enter" { SwitchToMode "Locked"; }
        bind "Esc" { UndoRenameTab; SwitchToMode "Locked"; }
    }

    renamepane {
        bind "Ctrl c" { SwitchToMode "Locked"; }
        bind "Enter" { SwitchToMode "Locked"; }
        bind "Esc" { UndoRenamePane; SwitchToMode "Locked"; }
    }

    shared_except "locked" {
        bind "Alt a" { SwitchToMode "Locked"; }
    }
}
